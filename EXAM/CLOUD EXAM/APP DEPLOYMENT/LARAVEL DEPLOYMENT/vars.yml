---
# Initial Server Setup
remote_user: "{{ mogbojuloluwa }}"

# MySQL Setup
mysql_root_password: "{{ damilola }}"
mysql_app_db: "{{ odesola }}"
mysql_app_user: "{{ ayomikun }}"
mysql_app_pass: "{{ bamise }}"

# Web Server Setup
http_host: "{{ mogbojuloluwa.me }}"
remote_www_root: "{{ /var/www/laravel }}"
app_root_dir: "{{ mogbojuloluwa.me }}"
document_root: "{{ /var/www/laravel }}/{{ mogbojuloluwa.me }/public"

# Laravel Env Variables
app_name: "{{  mogbojuloluwa.me }}"
app_env: "{{ local }}"
app_debug: "{{ true }}"
app_url: "{{ http://{{ http_host }}"
db_host: "{{ localhost }}"
db_port: "{{ 3306 }}"
db_database: "{{ mysql_app_db }}"
db_user: "{{ mysql_app_user }}"
db_pass: "{{ mysql_app_pass }}"

